# Video DNA Analyzer Bug 清单与优化清单

> 审计日期：2026-08-20  
> 审计分支：`master`  
> 审计提交：`75dbb22`  
> 审计方式：代码检查、Python/Electron 语法检查、依赖检查、自带样例实跑、导出与边界条件复现。

> 更新说明：本文的问题位置基于审计提交 `75dbb22`。随后进行的分层重构已修复或缓解多数问题，最新实施状态见下表；保留原始条目作为回归测试和验收依据。

## 1. 总体结论

原始审计版本虽然可以运行，但整体仍是单机单任务原型，并存在历史清理、接口安全、并发隔离、剪辑导出和桌面数据目录等高优先级问题。本轮已完成一次可运行的分层重构并关闭主要 P0/P1 缺陷；真实剪辑软件导入、系统凭据存储、动态端口、安装包签名和长视频压力测试仍是稳定版发布前的门槛。

本次实跑结果：

- 使用 Python 3.11 和仓库自带 `test_data/sample.mp4`。
- 在关闭在线 VLM、Whisper ASR 和用户插件的情况下，基础管线约 9.9 秒完成。
- 输出 4 个镜头、117.45 BPM、3 个硬切和 4 张关键帧。
- Python 与 Electron JavaScript 语法检查通过。
- `pip check` 通过，`npm audit` 未发现已知漏洞。
- macOS 运行时出现 OpenCV 与 PyAV 重复加载 FFmpeg 动态库的警告，可能导致偶发崩溃。

本次未覆盖：

- Whisper 模型下载与真实语音转写准确率。
- OpenAI、通义千问、Ollama 等真实模型调用。
- 剪映、Final Cut Pro、Premiere/Resolve 的实际导入验收。
- Windows/macOS/Linux 安装包完整安装测试。
- 长视频、高码率视频和多任务压力测试。

### 重构实施状态

| 问题 | 当前状态 |
|---|---|
| BUG-001 历史误删 | 已修复，并增加容量清理回归测试 |
| BUG-002 无认证/密钥暴露 | 已增加可选 Token；模型 API 已脱敏；插件仍只应安装可信代码 |
| BUG-003 API Key 全局环境变量 | 已修复，分析密钥改为请求级显式传递 |
| BUG-004 最近任务全局状态 | 已修复，后续操作统一使用 `session_id` |
| BUG-005 EDL 固定硬切 | 已修复并增加回归测试 |
| BUG-006 FCP XML 引用关键帧 | 已改为源视频 URI；仍需真实剪辑软件验收 |
| BUG-007 SRT `1000ms` | 已修复并增加边界测试 |
| BUG-008 模板越界/负时长 | 已修复并增加短视频高密度模板测试 |
| BUG-009 打包数据目录 | 已修复，Electron 通过 `VIDEODNA_DATA_DIR` 传递用户数据目录 |
| BUG-010 Electron Base64 大文件 | 已修复，改为主进程流式 multipart 上传 |
| BUG-011 上传无限制 | 已增加扩展名、流式大小和失败清理限制 |
| BUG-012 结果长期驻留内存 | 已修复，内存只保留有 TTL 的进度信息 |
| BUG-013 健康检查误认服务 | 已校验 `service` 标识；动态端口仍待实现 |
| BUG-014 Python 3 回退 | 已修复 |
| BUG-015 密钥明文配置 | 已限制配置文件权限；系统 Keychain 集成仍待实现 |
| BUG-016 依赖过重/无锁定范围 | 已拆分 base/asr/dev 并增加版本范围；完整锁文件仍待补充 |
| BUG-017 版本和文档 | 已统一为 0.3.1，补充 README、LICENSE、CHANGELOG |
| BUG-018 无测试 | 已增加首批模板、导出、存储和任务状态测试 |

## 2. Bug 清单

### P0：必须优先修复

#### BUG-001：历史目录清理可能误删全部旧记录

- 状态：已确认
- 位置：[`app/main.py`](app/main.py#L110-L131)
- 原因：代码先执行 `shutil.rmtree(d)`，随后再遍历已经被删除的目录计算大小。被删除目录的大小会被计算为 0，`total` 因此无法正确递减。
- 影响：当 `uploads` 总量超过 8GB 后，清理循环可能继续删除所有旧 session，只保留当前正在分析的目录。
- 修复建议：删除前保存目录大小，删除成功后从 `total` 扣除；每次删除后验证目录状态；为清理策略补充单元测试。
- 验收标准：准备 3 个超限目录，清理后仅删除达到容量目标所必需的最旧目录，其余记录保持完整。

#### BUG-002：HTTP 接口无认证，可泄露密钥并执行插件代码

- 状态：已确认
- 位置：[`app/main.py`](app/main.py#L978-L1096)、[`app/registry.py`](app/registry.py#L520-L568)
- 原因：接口没有访问令牌或用户鉴权；`/api/components` 直接返回完整模型配置，其中包含 `api_key`；插件接口可以安装 ZIP，分析时会直接导入并执行其中的 Python 文件。
- 影响：按照 README 使用 `0.0.0.0` 启动时，同一网络中的访问者可能读取 API Key、删除历史记录、写入导出目录、安装恶意插件并最终执行任意代码。
- 修复建议：
  - 桌面端默认只绑定 `127.0.0.1`。
  - 启动时生成随机会话令牌，所有写接口和敏感读接口必须校验。
  - API 响应中的密钥统一脱敏，只返回 `has_api_key`。
  - 插件安装增加显式信任确认、签名/哈希校验；生产模式可直接禁用插件安装。
- 验收标准：无令牌请求敏感接口返回 401/403；任何接口响应均不得出现完整 API Key。

#### BUG-003：API Key 使用进程级环境变量，并发时会串任务

- 状态：已确认
- 位置：[`app/main.py`](app/main.py#L89-L104)、[`app/main.py`](app/main.py#L227-L357)、[`app/analyzer/pipeline.py`](app/analyzer/pipeline.py#L246-L273)
- 原因：每个分析任务都会修改全局 `os.environ`。两个后台线程使用不同密钥时会相互覆盖。参数验证或上传保存失败发生在恢复逻辑之前时，密钥不会恢复。管线从模型配置读取密钥后也会直接写环境变量，但没有对应恢复。
- 影响：任务可能使用错误账号调用模型；密钥可能残留在进程环境中；并发调用存在隐私和计费风险。
- 修复建议：密钥作为显式参数传入模型客户端，不修改 `os.environ`；如第三方 SDK 必须依赖环境变量，应使用单任务进程隔离，不在线程之间共享。
- 验收标准：两个并发任务分别使用不同测试密钥时，请求头始终与各自任务一致；失败路径执行后环境变量不发生变化。

#### BUG-004：全局“最近一次任务”状态会导致结果串线

- 状态：已确认
- 位置：[`app/main.py`](app/main.py#L41-L50)、[`app/main.py`](app/main.py#L277-L281)、[`app/main.py`](app/main.py#L377-L390)、[`app/main.py`](app/main.py#L1053-L1067)
- 原因：关键帧、技能运行、BGM 推荐和剪映草稿等功能会回退到 `_last_session_id`、`_last_dna`、`_last_source_video`。
- 影响：多个任务并行时，用户 A 的操作可能读取或导出用户 B 最近完成的视频结果。
- 修复建议：所有后续操作必须显式携带 `session_id`；后端通过 session 存储读取结果；移除所有 `_last_*` 回退逻辑。
- 验收标准：交错运行两个 session 后，各自的技能、关键帧、BGM 推荐和草稿导出始终只使用指定 session。

### P1：核心功能错误

#### BUG-005：EDL 中所有转场都被导出为硬切

- 状态：已复现
- 位置：[`app/exporter.py`](app/exporter.py#L50-L82)
- 原因：代码计算了 `trans` 和 `trans_str`，但输出事件行仍然固定写死为 `C`。
- 复现：输入 `transition=dissolve` 的镜头，EDL 事件行仍为 `V C`，只有注释显示 `dissolve`。
- 影响：剪辑软件导入后丢失叠化、淡入淡出等转场信息。
- 修复建议：按 CMX3600 规范生成转场事件和持续帧数，并针对 cut/dissolve/fade 建立黄金文件测试。
- 验收标准：叠化镜头导出后事件类型与持续帧数正确，至少在一个主流剪辑软件中完成导入验证。

#### BUG-006：FCP7 XML 引用关键帧图片而不是源视频

- 状态：已确认
- 位置：[`app/exporter.py`](app/exporter.py#L130-L159)
- 原因：每个 `clipitem` 的 `pathurl` 被写为 `file://shots/shot_xxx.jpg`，没有引用实际源视频，也没有正确描述源素材的入点和出点。
- 影响：即使 XML 能被解析，也只会得到无效/缺失的静态图片素材，无法还原视频剪辑时间线。
- 修复建议：DNA 中保存源媒体 URI；FCP XML 使用同一个视频素材引用和正确的 `in/out/start/end`；转场应使用标准 transition item 结构。
- 验收标准：FCP/Resolve/Premiere 至少一种软件可导入并得到可播放、无离线素材的完整时间线。

#### BUG-007：SRT 时间戳可能产生非法的 1000 毫秒

- 状态：已复现
- 位置：[`app/exporter.py`](app/exporter.py#L251-L276)
- 原因：毫秒通过小数部分四舍五入，但没有把 `1000ms` 进位到下一秒。
- 复现：`1.9996` 秒被输出为 `00:00:01,1000`。
- 影响：部分播放器或剪辑软件会拒绝解析字幕或产生时间偏差。
- 修复建议：先将秒转换为整数毫秒，再依次拆分小时、分钟、秒和毫秒。
- 验收标准：任意非负时间均满足 `0 <= 毫秒 <= 999`，并覆盖秒/分/小时进位边界。

#### BUG-008：短视频套用高密度模板会产生越界和负时长

- 状态：已复现
- 位置：[`app/main.py`](app/main.py#L134-L201)
- 原因：内部边界强制满足 `prev + 0.3`，但没有限制在目标时长以内；过滤完成后又直接修改最后一段结束时间，可能把原本正时长的段改成负时长。
- 复现：10 镜头、10 秒模板套用到 1 秒目标视频时，生成的区间结束时间可达到 2.4 秒，最后一段时长为负数。
- 影响：前端时间轴越界，剪映草稿片段被跳过或生成异常工程。
- 修复建议：根据目标时长动态减少镜头数或降低最小时长；所有边界限定在 `[0, target_duration]`；最终统一重新构造连续区间并校验。
- 验收标准：任意输入都满足 `0 <= start < end <= target_duration`，区间有序、不重叠、无负数，并完整闭合到目标时长。

#### BUG-009：桌面打包版创建的数据目录没有被后端使用

- 状态：代码确认，需安装包复测
- 位置：[`app/main.py`](app/main.py#L37-L39)、[`electron/main.js`](electron/main.js#L59-L99)、[`electron/main.js`](electron/main.js#L236-L239)
- 原因：Electron 创建了 `app.getPath('userData')/uploads`，但没有通过参数或环境变量传给后端。后端仍使用相对于进程工作目录的 `uploads`。打包后进程工作目录为 resources 目录。
- 影响：安装到只读位置时后端可能启动失败；即使可写，也会将用户数据写进应用安装目录，影响升级、签名和卸载。
- 修复建议：Electron 设置 `VIDEODNA_DATA_DIR`；后端所有配置、上传、插件和日志统一基于该目录；CLI 模式再使用当前目录或显式参数。
- 验收标准：安装版运行后，所有可变数据只出现在系统用户数据目录，应用安装目录保持不变。

#### BUG-010：Electron 打开大视频会阻塞主进程并占用超量内存

- 状态：已确认
- 位置：[`electron/main.js`](electron/main.js#L282-L300)
- 原因：使用 `fs.readFileSync` 一次性读取最多 500MB 文件，然后转 Base64 并通过 IPC 发送。Buffer、Base64 字符串和 IPC 序列化会产生多份内存副本。
- 影响：大视频可能占用超过 1GB 内存，窗口长时间无响应甚至崩溃。
- 修复建议：不要把视频内容经 Base64/IPC 传给渲染进程；仅传文件路径，由后端流式读取或提供 Electron 自定义上传协议。
- 验收标准：上传 500MB 视频时 Electron 主进程保持响应，额外内存占用远低于文件大小。

### P2：稳定性与工程问题

#### BUG-011：上传接口缺少文件大小和类型限制

- 状态：已确认
- 位置：[`app/main.py`](app/main.py#L227-L259)
- 问题：后端接受任意扩展名和任意大小文件并直接写盘，没有 Content-Length 限制、流式累计限制、文件签名检查或剩余磁盘空间检查。
- 影响：无效文件会占满磁盘；暴露服务时可被用于拒绝服务攻击。
- 修复建议：配置上传上限；按块读取并累计大小；验证容器/魔数；上传前后检查可用磁盘空间；失败时删除半成品。

#### BUG-012：进度结果长期驻留内存

- 状态：已确认
- 位置：[`app/main.py`](app/main.py#L48-L77)、[`app/main.py`](app/main.py#L281-L281)
- 问题：每个 session 的完整 DNA 会保存到 `_progress[session_id]["result"]`，只有用户主动删除历史或清空历史时才释放。磁盘自动清理不会同步清理 `_progress`。
- 影响：长时间运行并分析大量视频后，进程内存持续增长。
- 修复建议：完成后只在内存保留状态和结果文件路径；设置 TTL/LRU；磁盘清理时同步移除内存状态。

#### BUG-013：端口健康检查可能误认其他本地服务

- 状态：已确认
- 位置：[`electron/main.js`](electron/main.js#L48-L57)
- 问题：只要 `127.0.0.1:8000/health` 返回 HTTP 200 就认为 Video DNA 后端已启动，没有校验 JSON 中的服务名称和版本。
- 影响：端口被其他服务占用时，桌面端可能连接错误服务并向其上传视频。
- 修复建议：校验 `service === "video-dna-analyzer"`；端口被占用时选择随机空闲端口并传给窗口。

#### BUG-014：macOS/Linux 开发模式的系统 Python 回退判断错误

- 状态：已确认
- 位置：[`electron/main.js`](electron/main.js#L85-L98)
- 问题：候选项包含字符串 `python3`，但判断只将字符串 `python` 视为 PATH 命令；`fs.existsSync('python3')` 通常返回 false。
- 影响：没有 `.venv/bin/python` 时，即使系统已安装 `python3`，Electron 仍提示找不到 Python。
- 修复建议：使用 `command -v`/`which` 语义检查 PATH 命令，或直接尝试 spawn 并处理失败。

#### BUG-015：配置密钥明文保存

- 状态：已确认
- 位置：[`app/registry.py`](app/registry.py#L150-L213)
- 问题：API Key 原样写入 `config.json`，未使用系统凭据存储，也未限制文件权限。
- 影响：其他本地用户、备份软件或恶意进程可能读取密钥。
- 修复建议：桌面端使用 macOS Keychain、Windows Credential Manager、Linux Secret Service；配置文件只保存凭据引用。

#### BUG-016：依赖范围过宽且基础功能被迫安装重型依赖

- 状态：已确认
- 位置：[`requirements.txt`](requirements.txt)
- 问题：Python 依赖大多只有最低版本，没有上限或锁文件；基础分析也必须安装 faster-whisper、ONNX Runtime、PyAV、OpenCV、librosa/Numba 全套依赖。本次安装时 `scenedetect 0.7.1` 还提示不再提供 `opencv` extra。
- 影响：构建不可复现、安装慢、磁盘占用大、升级后容易出现兼容问题。macOS 实跑出现 OpenCV 与 PyAV 携带的 FFmpeg 动态库重复加载警告。
- 修复建议：拆分 `base`、`asr`、`desktop`、`dev` 可选依赖；使用 lock/constraints 文件；CI 固定验证 Python 3.10/3.11/3.12。

#### BUG-017：版本和文档与代码不一致

- 状态：已确认
- 位置：[`README.md`](README.md)、[`package.json`](package.json)、[`app/main.py`](app/main.py#L20-L20)
- 问题：仓库已有 `v0.3.0` 标签，但应用和包版本仍为 `0.2.0`；README 示例使用不存在的 `--describe`，实际 CLI 只有 `--no-describe`；README 声称 MIT，但仓库没有 LICENSE 文件；README 仍把已经实现的剪映导出列为后续方向。
- 影响：用户无法可靠安装和判断版本，命令示例直接报错，许可证信息不完整。
- 修复建议：建立单一版本来源并在构建时注入；CI 验证 README 命令；补充正式 LICENSE。

#### BUG-018：缺少自动化测试

- 状态：已确认
- 位置：仓库全局
- 问题：仓库包含测试视频，但没有单元测试、接口测试、导出黄金文件测试和安装包冒烟测试。
- 影响：清理误删、非法时间戳、模板负时长和 EDL 转场丢失等问题无法在提交阶段被发现。
- 修复建议：优先为纯函数和已复现 Bug 建立回归测试，然后增加样例视频端到端测试。

## 3. 优化清单

### 3.1 安全与数据保护

- [x] 后端默认只监听 `127.0.0.1`，文档移除默认 `0.0.0.0` 示例。
- [x] 桌面端启动时生成随机访问令牌，并注入后端和前端。
- [x] 所有写接口、历史接口、模型接口、插件接口增加鉴权。
- [x] API 返回模型信息时移除 `api_key`，仅返回是否已配置。
- [ ] API Key 改用系统凭据存储。
- [ ] 插件增加可信来源提示、签名/哈希和权限说明。
- [ ] 增加上传大小、媒体类型、ZIP 解压总量和磁盘空间限制。
- [x] 移除 `/api/export` 的任意服务器目录写入能力，仅返回受控下载文件。

### 3.2 任务与并发架构

- [x] 移除 `_last_session_id`、`_last_dna`、`_last_source_video`。
- [x] 所有操作显式传入 `session_id`。
- [ ] 使用任务仓库统一保存 `queued/running/succeeded/failed/cancelled` 状态。
- [ ] 使用受控线程池或进程池，限制同时运行的 CPU/GPU 重任务数量。
- [ ] 增加取消任务、超时、失败重试和应用退出时的任务收尾。
- [x] 进度状态使用 TTL/LRU，完整结果只持久化到磁盘或数据库。
- [x] API Key 和模型客户端保持请求级隔离，不修改全局环境变量。

### 3.3 分析准确率

- [ ] 为镜头检测提供阈值预设和可视化调参。
- [ ] 评估 TransNet V2，改善叠化、闪白和渐变转场检测。
- [ ] ASR 模型常驻复用，避免每次分析重新创建 `WhisperModel`。
- [ ] 区分“ASR 失败”和“能量检测降级”，在结果中明确标记方法与错误。
- [ ] 能量检测不要将普通 BGM 直接标记为语音区域。
- [ ] 为 BPM、卡点、转场、景别和运镜输出置信度及算法版本。
- [ ] 使用真实短剧、Vlog、游戏、舞蹈、无音轨视频建立评测集。

### 3.4 导出与剪辑工程

- [ ] 修复 EDL 转场事件、时间码和 29.97/59.94 FPS 处理。
- [ ] FCP7 XML 引用真实源媒体并补齐标准 rate、file、link、audio 结构。
- [x] 修复 SRT 毫秒进位，并增加进位边界回归测试。
- [ ] 剪映草稿增加版本兼容层，不把单一 `DRAFT_VERSION` 当作长期稳定格式。
- [ ] 导出前执行统一 DNA schema 校验和时间区间校验。
- [ ] 为每种导出格式加入黄金文件测试和真实软件导入测试。
- [ ] 明确区分“分析标记文件”和“可恢复原视频时间线工程”。

### 3.5 桌面端和大文件性能

- [x] Electron 只传文件路径，不通过 IPC 传 Base64 视频。
- [ ] 上传和文件复制改为流式处理，并显示速度、剩余时间和取消按钮。
- [ ] 关键帧、视频预览和历史列表使用懒加载。
- [ ] 后端端口使用动态分配并严格校验服务身份。
- [x] 将用户数据、缓存、日志、模型和导出目录统一放到平台规范目录。
- [ ] 为视频、音频和模型缓存分别设置容量上限和清理策略。

### 3.6 依赖、构建与发布

- [x] 使用 Python 3.11 作为当前推荐运行版本。
- [x] 拆分基础、ASR 和开发依赖。
- [ ] 引入锁文件或 constraints，确保 CI 与本地依赖一致。
- [ ] 处理 macOS OpenCV/PyAV FFmpeg 动态库冲突。
- [ ] Windows 安装包签名；macOS 完成签名、公证和 Gatekeeper 验证。
- [ ] 安装包 CI 增加启动、上传样例、分析、历史和导出冒烟测试。
- [x] 统一应用代码、安装包和 API `/health` 版本为 `0.3.1`。
- [x] 补充 LICENSE、CHANGELOG 和升级/数据迁移说明。

### 3.7 代码结构与可维护性

- [x] 将 `app/main.py` 拆分为 analysis/media/export/studio/components 路由和 service 层。
- [x] 将 `app/static/index.html` 拆分为 HTML、CSS 和按业务划分的 JavaScript 模块。
- [ ] 使用 Pydantic 定义 DNA、镜头、音频、剪辑方案和 API 请求响应模型。
- [ ] 为文件系统、模型调用、任务状态和插件系统定义清晰接口。
- [ ] 禁止大范围 `except Exception: pass`，保留结构化错误和可观测日志。
- [ ] 引入 Ruff、Pyright/Mypy、ESLint/Prettier 和 pre-commit。

## 4. 建议修复顺序

### 第一阶段：阻断数据与安全风险

1. 修复 BUG-001 历史误删。
2. 修复 BUG-002 接口鉴权和密钥泄露。
3. 修复 BUG-003 环境变量密钥并发问题。
4. 修复 BUG-004 全局最近任务串线。
5. 修复 BUG-009 打包版数据目录。

### 第二阶段：保证核心输出正确

1. 修复 BUG-005 EDL 转场。
2. 重做 BUG-006 FCP7 XML 源媒体结构。
3. 修复 BUG-007 SRT 时间戳。
4. 修复 BUG-008 模板边界。
5. 建立导出黄金文件和真实软件导入测试。

### 第三阶段：性能和工程化

1. 修复 BUG-010 大文件 Base64/IPC。
2. 增加上传限制、任务队列、取消和资源控制。
3. 拆分依赖并固定版本。
4. 完善自动化测试、签名、公证和发布流程。
5. 拆分后端路由和前端单文件。

## 5. 建议测试清单

### 单元测试

- [x] 历史容量清理：只删除达到上限所需的旧目录，并保留活动任务。
- [x] `_build_cut_plan`：短视频、高密度模板、连续边界和节拍吸附。
- [ ] `_tc`：24/25/30/29.97/59.94 FPS 和跨小时视频。
- [x] `_srt_ts`：覆盖 `1.9996` 的毫秒进位回归。
- [ ] `_safe_join`：相对穿越、绝对路径、Windows 盘符、Unicode 文件名。
- [ ] 插件 ZIP：zip-slip、解压炸弹、重复 ID、非法 entry。

### 接口测试

- [ ] 未认证访问敏感接口必须失败。
- [ ] 两个 session 并发分析时结果和密钥不串线。
- [ ] 超大上传、伪装扩展名、损坏媒体和磁盘不足时能安全失败并清理文件。
- [ ] 任务完成、失败、取消和超时状态一致。
- [ ] 删除历史不会删除正在运行的任务。

### 端到端测试

- [x] 自带样例完成上传、分析、关键帧、历史、Range 播放和全部格式导出。
- [ ] 无音轨视频、纯音乐视频、纯语音视频、可变帧率视频和 29.97 FPS 视频。
- [ ] 至少 1 小时长视频和 500MB 大视频。
- [ ] Windows/macOS 安装版首次启动和升级后数据保留。
- [ ] 剪映草稿、EDL 和 FCP XML 在真实剪辑软件中导入验证。

## 6. 发布门槛建议

在对外发布稳定版本前，至少满足：

- [ ] 所有 P0、P1 Bug 关闭并有回归测试。
- [ ] 后端不再依赖进程级最近任务状态和 API Key 环境变量。
- [ ] 敏感接口有认证，接口响应不泄露密钥。
- [ ] EDL、FCP XML、SRT 和剪映草稿经过真实软件导入验证。
- [ ] Windows/macOS 安装包通过全新机器冒烟测试。
- [ ] 有完整 LICENSE、CHANGELOG、版本号和数据迁移说明。
- [ ] CI 自动执行语法、静态检查、单元测试、端到端样例和安装包启动测试。
